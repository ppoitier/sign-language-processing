from: "io.yaml"


experiment:
  variant: "mstcn_io_no_refinement"
  seed: random


training:
  loss_functions:
    classification:
      name: "cross-entropy"
      use_weights: false
      multi_layer: false